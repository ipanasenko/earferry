/**
 * Liberapay, not Ko-fi or Buy Me a Coffee: it is the one platform whose model is
 * donations only, so nothing here is a promise of recompense. A personal profile
 * rather than an earferry one, deliberately, because a gift to a person reads
 * differently from revenue for a product, and it outlives this project.
 *
 * Donating must never affect access. That is what keeps this a gift rather than
 * a price, and keeps EarFerry outside KVK's "je vraagt een prijs of tarief"
 * test. Never surface it on the waitlist page.
 *
 * See docs/research/billing-options.md.
 */
export const DONATE_URL = "https://liberapay.com/ipanasenko/donate";
